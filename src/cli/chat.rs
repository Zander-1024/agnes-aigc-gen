use anyhow::Result;

pub fn run() -> Result<()> {
    eprintln!("Chat is not implemented yet.");
    eprintln!("Use `agnes-aigc-gen dashboard` for the terminal UI.");
    eprintln!("Phase 2 will integrate pi_agent_rust for streaming chat.");
    Ok(())
}
